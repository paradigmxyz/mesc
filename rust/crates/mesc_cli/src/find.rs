use crate::MescCliError;

pub(crate) fn find_command(
    _chain_id: Option<u64>,
    _name: Option<String>,
    _url: Option<String>,
    _metadata: Vec<String>,
) -> Result<(), MescCliError> {
    todo!()
}
